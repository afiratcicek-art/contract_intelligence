# ADR-0002: migration-runner bağlantısı. Gerçek değeri buraya YAZMA — bu dosya committed.
# Gerçek connection-string yalnız .env.migrations'ta (gitignored). Format:
# MIGRATION_DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DBNAME
MIGRATION_DATABASE_URL=
